/** The only legal high-level game flow states for V1. */
export enum GameState {
  Boot = 'boot',
  Home = 'home',
  Ready = 'ready',
  Playing = 'playing',
  Paused = 'paused',
  GameOver = 'game-over',
}

